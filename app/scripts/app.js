angular
    .module('BlocTime', ['firebase'])
    .config(function(){
    //will need to configure different states ie. $locationProvider, $stateProvider
    //will need to configure firebase 
    
});